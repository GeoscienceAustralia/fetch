#!/usr/bin/env bash

# Load such environment vars if present (eg. proxy settings):
[ -e /etc/neo-env.sh ] && . /etc/neo-env.sh

# This will block and fetch data given a config file.
exec /usr/local/bin/python2.7 -m fetch.auto "$@"
